// Rename this file 'config.js' and fill in your API key

// App settings
var config = {
  apiKey: '' // Your API key
};

// Domino settings
domino.settings({
  verbose: true,
  mergeRequests: true
});
